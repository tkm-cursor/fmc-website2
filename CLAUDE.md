# FMC株式会社コーポレートサイト

## プロジェクト概要

FMC株式会社のコーポレートサイト。経営理念「縁が縁を呼ぶ」のもと、人との繋がり・輪を大切にする企業姿勢を体現したデザイン。

- **デプロイ先**: Cloudflare Pages
- **URL**: https://fmc-website2.pages.dev

## 技術スタック

- HTML（セマンティック必須）
- CSS（カスタムプロパティで一元管理、Mobile-first）
- Vanilla JavaScript（IIFE形式、フレームワーク不使用）

フレームワークの導入が必要と判断した場合は、実装前に必ず提案・相談すること。

## ファイル構成

```
FMC_Website/
├── index.html      # 1ページ完結のコーポレートサイト
├── styles.css      # Mobile-first CSS
├── script.js       # IIFE形式のJavaScript
└── uploads/        # 画像素材（9枚）
```

## デザインルール

### カラーパレット

| 役割 | 変数名 | 値 |
|------|--------|----|
| メインカラー | `--color-primary` | `#14223D`（ディープネイビー） |
| アクセントカラー | `--color-accent` | `#4A90FF`（明るいブルー） |
| 強調色 | `--color-highlight` | `#FF6B35`（オレンジ） |

**強調色 `#FF6B35` の使用箇所はContactセクションの「お問い合わせはこちら」ボタンのみ。**  
カラー方針から逸脱する変更を求められた場合は必ず警告すること。

### フォント

- 本文：Noto Sans JP
- 英字：DM Sans

## 実装ルール

- **Mobile-first**：768px以下を優先設計し、それ以上はメディアクエリで拡張
- **セマンティックHTML必須**：`<header>`, `<main>`, `<section>`, `<footer>` 等を適切に使用
- **アクセシビリティ配慮**：`aria-label`, `aria-hidden`, `role` 等の属性を適宜付与
- **CSSカスタムプロパティ**：色・フォント・スペーシング等は `:root` で一元管理
- **インラインスタイル禁止**：`style=""` 属性は使用しない

## 未確定事項（仮置き中）

| 項目 | 現状 | 備考 |
|------|------|------|
| 代表メールアドレス | `info@fmc-co.net` | 確定済み |
| OGP画像 | 未作成 | `<meta property="og:image">` は仮のまま |
| Favicon | 未作成 | `<link rel="icon">` は仮のまま |

## 修正時のルール

1. **大きな構造変更**（レイアウト刷新、セクション順序の大幅変更等）は実装前に提案を求めること
2. **セクションの追加・削除**は先に意図を確認すること
3. **カラー方針からの逸脱**（新色の追加、既存色の変更等）は必ず警告すること
